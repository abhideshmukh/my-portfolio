import React from "react";
import Tilt from "react-parallax-tilt";
import { motion } from "framer-motion";

import { styles } from "../styles";
import { services } from "../constants";
import { SectionWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";

const ServiceCard = ({ index, title, icon }) => {
  return (
    <Tilt
      className="parallax-effect-glare-scale xs:w-[250px] w-full"
      style={{borderRadius:'20px'}}
      perspective={500}
      glareEnable={true}
      glareMaxOpacity={0.45}
      scale={1.02}>
      <div
        variants={fadeIn("right", "spring", 0.5 * index, 0.75)}
        className="w-full green-pink-gradient p-[1px] rounded-[20px] "
      >
        <div
          options={{ max: 45, scale: 1, speed: 450 }}
          className="roundex-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col"
        >
          <img
            src={icon}
            alt="web-development"
            className="w-16 h-16 object-contain"
          />

          <h3 className="text-white text-[20px] font-bold text-center">
            {title}
          </h3>
        </div>
      </div>
    </Tilt>
  );
};

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview.</h2>
      </motion.div>
      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className="mt-4 text-secondary text-[17px] max-w-3xl leading-[30px]"
      >
        Experienced Software Developer with a demonstrated history of working in various domains of the Information Technology and services industry. Skilled in JavaScript, Angular, Ionic Framework, and React. Strong engineering professional with a Master’s Degree focused in Computer Science.
        <br/> <br/>I have demonstrated excellent planning, organizing and seamless deliveries of high-performance and robust features in every project release, adhering to all defined standards and timeline.
      </motion.p>

      <div className="mt-20 flex flex-wrap gap-10">
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");
